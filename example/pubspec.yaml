name: example
description: A demo for hyper_snackbar.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  hyper_snackbar: ^0.2.4
  go_router: ^17.0.1